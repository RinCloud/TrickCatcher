#include <iostream>
using namespace std;

int main() {
    int X, Y, Z;
    cin >> X >> Y >> Z;

    int gap = Y + 2 * Z;
    int maxPeople = (X - Z) / gap;

    cout << maxPeople << endl;

    return 0;
}