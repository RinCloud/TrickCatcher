#include <iostream>
using namespace std;

int main() {
    int X, Y, Z;
    cin >> X >> Y >> Z;

    int gap = Y + 2 * Z; // total space occupied by a person
    int maxPersons = (X - Z) / gap; // maximum number of persons that can sit
    cout << maxPersons << endl;

    return 0;
}