#include <iostream>
using namespace std;

int main() {
    int X, Y, Z;
    cin >> X >> Y >> Z;

    int interval = Y + 2 * Z;
    int maxPeople = (X - Z) / interval;

    cout << maxPeople << endl;

    return 0;
}