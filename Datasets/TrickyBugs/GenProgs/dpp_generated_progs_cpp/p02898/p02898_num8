#include <iostream>
using namespace std;

int main() {
    int N, K;
    cin >> N >> K;
    
    int count = 0;
    for(int i=0; i<N; i++) {
        int height;
        cin >> height;
        if(height >= K) {
            count++;
        }
    }
    
    cout << count;
    
    return 0;
}