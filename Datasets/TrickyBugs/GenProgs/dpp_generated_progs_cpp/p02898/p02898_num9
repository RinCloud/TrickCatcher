#include <iostream>

int main() {
    int N, K;
    std::cin >> N >> K;

    int count = 0;
    for (int i = 0; i < N; i++) {
        int h;
        std::cin >> h;
        if (h >= K) {
            count++;
        }
    }

    std::cout << count << std::endl;

    return 0;
}