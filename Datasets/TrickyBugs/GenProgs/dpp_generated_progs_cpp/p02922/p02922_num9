#include <iostream>

int main() {
    int A, B;
    std::cin >> A >> B;

    int powerStripsRequired = (B - 1) / (A - 1);
    if ((B - 1) % (A - 1) != 0) {
        powerStripsRequired++;
    }

    std::cout << powerStripsRequired << std::endl;

    return 0;
}