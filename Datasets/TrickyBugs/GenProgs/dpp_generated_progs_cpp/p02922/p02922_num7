#include <iostream>

int main() {
    int A, B;
    std::cin >> A >> B;
    
    int count = 0;
    while (B > 0) {
        B -= (A - 1);
        count++;
    }
    
    std::cout << count << std::endl;
    
    return 0;
}