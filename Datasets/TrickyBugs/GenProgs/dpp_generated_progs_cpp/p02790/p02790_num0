#include <iostream>
#include <string>
#include <algorithm>

int main() {
    int a, b;
    std::cin >> a >> b;
    
    std::string str_a(b, '0' + a);
    std::string str_b(a, '0' + b);
    
    std::cout << std::min(str_a, str_b) << std::endl;
    
    return 0;
}