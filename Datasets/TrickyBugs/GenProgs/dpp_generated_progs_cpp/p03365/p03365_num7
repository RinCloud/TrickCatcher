#include <iostream>

const int MOD = 1000000007;

int main() {
    int N;
    std::cin >> N;
    
    long long answer = 1;
    for (int i = 2; i <= N; i++) {
        answer = (answer * i) % MOD;
    }
    
    std::cout << answer << std::endl;
    
    return 0;
}