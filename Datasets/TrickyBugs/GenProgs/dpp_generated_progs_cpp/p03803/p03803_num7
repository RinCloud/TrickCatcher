```cpp
#include <iostream>

int main() {
    int A, B;
    std::cin >> A >> B;

    if (A == B) {
        std::cout << "Draw";
    } else if (A == 1 || (A != 1 && B != 1 && A > B)) {
        std::cout << "Alice";
    } else {
        std::cout << "Bob";
    }

    return 0;
}
```