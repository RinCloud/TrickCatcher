#include <iostream>
using namespace std;

int main() {
    int N, K;
    cin >> N >> K;

    long long int ans = K;

    if (N > 1) {
        if (N % 2 == 0) {
            ans += K;
        } else {
            ans += K * K;
        }
    }

    cout << ans << endl;

    return 0;
}