#include <iostream>
#include <cmath>

using namespace std;

int main() {
    long long x;
    cin >> x;

    // Calculate the minimum number of operations needed to score at least x points
    long long operations = ceil(x / 14.0);

    cout << operations << endl;

    return 0;
}