#include <iostream>
using namespace std;

int main() {
    long long X, Y;
    cin >> X >> Y;
    
    int length = 1;
    while (X * 2 <= Y) {
        length++;
        X *= 2;
    }
    
    cout << length << endl;
    
    return 0;
}