#include<bits/stdc++.h>
using namespace std;

int main() {
    long long X, Y;
    cin >> X >> Y;

    int maxLength = 1;

    while(X * 2 <= Y) {
        X *= 2;
        maxLength++;
    }

    cout << maxLength << endl;

    return 0;
}