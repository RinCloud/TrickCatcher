#include <iostream>
using namespace std;

int main() {
    int A, B, C, D, E;
    cin >> A >> B >> C >> D >> E;
    
    int time = 0;
    
    // Order ABC Don
    time += (A + 9) / 10 * 10;
    
    // Order ARC Curry
    time += (B + 9) / 10 * 10;
    
    // Order AGC Pasta
    time += (C + 9) / 10 * 10;
    
    // Order APC Ramen
    time += (D + 9) / 10 * 10;
    
    // Order ATC Hanbagu
    time += (E + 9) / 10 * 10;
    
    cout << time << endl;
    
    return 0;
}