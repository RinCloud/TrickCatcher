#include <iostream>

int main() {
    int N, A, B;
    std::cin >> N >> A >> B;

    int min_sum = A * (N - 1) + B;
    int max_sum = A * (N - 1) + B;

    if (A != B) {
        std::cout << max_sum - min_sum + 1 << std::endl;
    } else {
        std::cout << 1 << std::endl;
    }

    return 0;
}