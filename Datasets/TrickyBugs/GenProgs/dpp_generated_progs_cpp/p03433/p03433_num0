#include <iostream>

int main() {
    int N, A;
    std::cin >> N >> A;

    if ((N % 500 <= A) || (N % 500 == 0)) {
        std::cout << "Yes" << std::endl;
    } else {
        std::cout << "No" << std::endl;
    }

    return 0;
}