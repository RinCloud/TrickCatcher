#include <iostream>
using namespace std;

int main() {
    int N, A;
    cin >> N >> A;
    
    int remaining = N - A * 500;
    
    if (remaining <= 0 || remaining % 1 == 0) {
        cout << "Yes" << endl;
    } else {
        cout << "No" << endl;
    }
    
    return 0;
}