#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int N, K;
    cin >> N >> K;

    int min_diff = 0;
    if (N % K != 0) {
        min_diff = 1;
    }

    cout << min_diff << endl;

    return 0;
}