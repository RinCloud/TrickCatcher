#include <iostream>
#include <algorithm>

int main() {
    int a, b, c;
    std::cin >> a >> b >> c;
    
    int min_diff = std::abs((a * b * c) - (a * b) - (a * c) - (b * c)) / 2;

    std::cout << min_diff << std::endl;

    return 0;
}