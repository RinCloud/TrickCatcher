#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int A, B, C;
    cin >> A >> B >> C;
    
    long long ans = min({abs(A-B)+abs(B-C), abs(A-C)+abs(B-C), abs(A-B)+abs(A-C)});
    cout << ans << endl;

    return 0;
}