#include<iostream>
#include<algorithm>

using namespace std;

int main() {
    long long int A, B, C;
    cin >> A >> B >> C;

    long long int min_difference = min({A*B, B*C, C*A}) - (A*B*C)/2;
    cout << min_difference << endl;

    return 0;
}