#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int A, B, C;
    cin >> A >> B >> C;
    
    int diff = 0;
    diff = max(diff, abs(A - B));
    diff = max(diff, abs(B - C));
    diff = max(diff, abs(C - A));
    
    cout << diff * 2 << endl;
    
    return 0;
}