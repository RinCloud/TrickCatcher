#include <iostream>
#include <string>

int main() {
    int N;
    std::string s, t;
    
    // Read input
    std::cin >> N >> s >> t;
    
    // Calculate length of shortest string
    int shortestLength = N + (N - 1);
    
    std::cout << shortestLength << std::endl;
    
    return 0;
}