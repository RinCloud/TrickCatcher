#include <iostream>
#include <set>

int main() {
    int N;
    std::cin >> N;
    
    std::set<int> diameters;
    for (int i = 0; i < N; ++i) {
        int d;
        std::cin >> d;
        diameters.insert(d);
    }
    
    std::cout << diameters.size() << std::endl;
    
    return 0;
}