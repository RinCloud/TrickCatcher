#include <iostream>
#include <unordered_set>

int main() {
    int N;
    std::cin >> N;

    std::unordered_set<int> mochiSet;

    for (int i = 0; i < N; i++) {
        int d;
        std::cin >> d;
        mochiSet.insert(d);
    }

    std::cout << mochiSet.size() << std::endl;

    return 0;
}