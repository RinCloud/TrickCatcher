#include <iostream>
#include <algorithm>

int main() {
    int W, a, b;
    std::cin >> W >> a >> b;

    int distance = std::abs(b - a) - W;
    std::cout << std::max(distance, 0) << std::endl;

    return 0;
}