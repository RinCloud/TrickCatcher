#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int W, a, b;
    cin >> W >> a >> b;
    
    int dist = max(a, b) - min(a+W, b+W);
    cout << max(0, dist) << endl;
    
    return 0;
}