#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int W, a, b;
    cin >> W >> a >> b;
    
    int minDistance = max(0, abs(b - a) - W);
    cout << minDistance << endl;
    
    return 0;
}