#include <iostream>

int main() {
    int X, A, B;
    std::cin >> X >> A >> B;
    
    if (B - A <= 0) {
        std::cout << "delicious\n";
    } else if (B - A <= X) {
        std::cout << "safe\n";
    } else {
        std::cout << "dangerous\n";
    }
    
    return 0;
}