#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;
    
    int quotient = N / 111;
    int remainder = N % 111;
    
    if (remainder > 0) {
        quotient += 1;
    }
    
    cout << quotient * 111 << endl;
    
    return 0;
}