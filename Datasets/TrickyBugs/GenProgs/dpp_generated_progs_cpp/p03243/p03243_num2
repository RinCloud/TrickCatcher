#include <iostream>

int main() {
    int N;
    std::cin >> N;
    
    int answer = (N / 111) * 111;
    if (answer < N) {
        answer += 111;
    }
    
    std::cout << answer << std::endl;
    
    return 0;
}