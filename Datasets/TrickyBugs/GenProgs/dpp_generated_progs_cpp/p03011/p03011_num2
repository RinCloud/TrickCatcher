#include <iostream>
#include <algorithm>

int main() {
    int P, Q, R;
    std::cin >> P >> Q >> R;

    int min_sum = std::min(P + Q, std::min(Q + R, R + P));
    std::cout << min_sum << std::endl;

    return 0;
}