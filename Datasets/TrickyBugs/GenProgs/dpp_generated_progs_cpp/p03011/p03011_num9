#include <iostream>
#include <algorithm>

int main() {
    int P, Q, R;
    std::cin >> P >> Q >> R;

    // Calculate the minimum sum of flight times
    int sum = std::min(P + Q, std::min(Q + R, R + P));

    std::cout << sum << std::endl;

    return 0;
}