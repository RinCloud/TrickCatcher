#include <iostream>
#include <algorithm>

int main() {
    int P, Q, R;
    std::cin >> P >> Q >> R;

    int minSum = std::min(P + R, std::min(P + Q, Q + R));
    std::cout << minSum << std::endl;

    return 0;
}