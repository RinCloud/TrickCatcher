#include <iostream>
using namespace std;

int main() {
  int N, X, T;
  cin >> N >> X >> T;

  int batches = N / X;
  if (N % X != 0) {
    batches++;
  }

  int totalMinutes = batches * T;
  cout << totalMinutes << endl;

  return 0;
}