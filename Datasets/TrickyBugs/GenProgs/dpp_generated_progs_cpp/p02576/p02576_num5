#include <iostream>
using namespace std;

int main() {
    int N, X, T;
    cin >> N >> X >> T;

    int batches = (N + X - 1) / X;
    int time = batches * T;

    cout << time << endl;

    return 0;
}