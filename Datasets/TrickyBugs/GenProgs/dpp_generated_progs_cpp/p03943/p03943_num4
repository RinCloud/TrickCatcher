#include <iostream>

int main() {
    int a, b, c;
    std::cin >> a >> b >> c;

    if ((a + b + c) % 2 == 0 && (a + b + c) / 2 == a || (a + b + c) / 2 == b || (a + b + c) / 2 == c)
        std::cout << "Yes" << std::endl;
    else
        std::cout << "No" << std::endl;

    return 0;
}