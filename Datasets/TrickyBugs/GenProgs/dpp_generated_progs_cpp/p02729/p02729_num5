#include <iostream>

int main() {
    int N, M;
    std::cin >> N >> M;

    int even_count = N * (N - 1) / 2; // choose 2 balls out of N even balls
    int odd_count = M * (M - 1) / 2; // choose 2 balls out of M odd balls

    std::cout << even_count + odd_count << std::endl;

    return 0;
}