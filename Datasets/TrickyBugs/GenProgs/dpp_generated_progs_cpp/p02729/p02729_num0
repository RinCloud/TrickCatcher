#include <iostream>

int main() {
    int N, M;
    std::cin >> N >> M;

    int count = 0;
    if (N > 0 && M > 0) {
        count = N * M;
    }

    std::cout << count << std::endl;

    return 0;
}