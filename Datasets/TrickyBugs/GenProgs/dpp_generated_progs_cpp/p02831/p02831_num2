#include <iostream>
#include <algorithm>

int main() {
    int A, B;
    std::cin >> A >> B;
    
    int gcd = std::__gcd(A, B);
    int lcm = (A * B) / gcd;
    int min_pieces = lcm;
    
    std::cout << min_pieces << std::endl;
    
    return 0;
}