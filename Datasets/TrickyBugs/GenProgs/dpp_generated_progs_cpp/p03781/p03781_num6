#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int X;
    cin >> X;
    
    int t = floor(sqrt(2*X));
    while (t*(t+1)/2 < X) {
        t++;
    }
    
    cout << t << endl;
    
    return 0;
}