#include <iostream>

int main() {
    int N;
    std::cin >> N;
    
    int totalSeats = 0;
    for (int i = 0; i < N; i++) {
        int l, r;
        std::cin >> l >> r;
        
        totalSeats += (r - l + 1);
    }
    
    std::cout << totalSeats << std::endl;
    
    return 0;
}