#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int H, W;
    cin >> H >> W;
    
    int ans = min(min((W+2)/3, W/3)*H, min((H+2)/3, H/3)*W);
    
    cout << ans << endl;
    
    return 0;
}