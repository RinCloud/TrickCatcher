#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int Q;
    cin >> Q;

    for (int i = 0; i < Q; i++) {
        long long A, B;
        cin >> A >> B;
        long long max_score = (A - 1) * (B - 1);

        cout << max_score << endl;
    }

    return 0;
}