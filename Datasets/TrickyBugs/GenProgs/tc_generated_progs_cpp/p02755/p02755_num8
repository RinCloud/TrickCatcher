#include <iostream>

int main() {
    int A, B;
    std::cin >> A >> B;
    int i = 1;
    while (i < 9999) {
        if (i * 8 / 100 == A && i * 10 / 100 == B) {
            std::cout << i;
            return 0;
        }
        i++;
    }
    std::cout << -1;
    return 0;
}