**REPAIRED CODE**:

#include"bits/stdc++.h"
using namespace std;
int main(){
	long long N, A, B;
	cin >> N >> A >> B;
	if(B < A || (B-A)*(N-2)+B < A){
		cout << 0;
		return 0;
	}
	cout << (B-A)*(N-2)+1;
}