**REPAIRED CODE**:

```cpp
#include<bits/stdc++.h>
using namespace std;
int a,b;
string s1,s2,s3; 
int main() {
	cin>>s1>>s2;
	cin>>a>>b;
	cin>>s3;
	if(s1==s3) a--;
	else b--;
	cout<<a<<" "<<b;
	return 0;
}
```

Explanations:
- In the original code, `s1[0]==s3[0]` is used for comparing strings `s1` and `s3`. But it is incorrect because it compares only the first character of the two strings. In the repaired code, `s1==s3` is used for comparing the entire strings `s1` and `s3`.