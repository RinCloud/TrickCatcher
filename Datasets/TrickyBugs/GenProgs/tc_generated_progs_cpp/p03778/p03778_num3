#include<bits/stdc++.h>
using namespace std;
int main(){
    int W,a,b;
    cin>>W>>a>>b;
    int R;
    if(a+W<b){
        R=b-(a+W); 
        cout<<R;
    }
    else if(b<=a+W && b>=a){
        cout<<0;
    }
    else{
        cout<<a-(b+W);
    }
    return 0;
}