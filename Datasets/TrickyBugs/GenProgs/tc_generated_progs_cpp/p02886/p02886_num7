**REPAIRED CODE**:

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;
  int a,b,s = 0;  // Added initialization for s
  cin >> b;
  for ( int i=0; i<N-1; ++i) {
    cin >> a;
    s += a*b;
    b +=a;
  }
  
  cout << s;
}
```

Explanation:
1. Added initialization for the variable `s` to avoid undefined behavior.
2. Replaced `int a,b,c,s;` with `int a,b,s = 0;` to fix the uninitialized variable `s`.
3. Code should compile successfully now and provide the correct output.