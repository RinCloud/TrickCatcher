**REPAIRED CODE**:

```cpp
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, m, a = 0;
    cin >> n >> m;
    a += (n * (n - 1)) / 2;
    a += (m * (m - 1)) / 2;
    cout << a;
    return 0;
}
```