```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int x, y, a = 1, i = 0; // Fixed initialization of i
    cin >> x >> y;
    while (a <= x) { // Fixed loop condition
        a *= y;
        i++;
    }
    cout << i << endl;
}
```