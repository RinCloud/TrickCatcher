#include<bits/stdc++.h>
using namespace std;
char a[100005];
int main()
{
    cin>>a;
    int len=strlen(a);
    int ans = 0; // Initialize ans to 0
    for(int i=1;i<len;i++)
	    if(a[i]!=a[i-1])
	        ans++;
    cout<<ans<<endl;
    return 0;
}