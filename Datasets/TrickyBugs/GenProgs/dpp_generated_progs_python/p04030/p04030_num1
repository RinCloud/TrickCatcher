s = input()

editor_string = ""

for key in s:
    if key == '0':
        editor_string += '0'
    elif key == '1':
        editor_string += '1'
    else:
        if editor_string:
            editor_string = editor_string[:-1]

print(editor_string)