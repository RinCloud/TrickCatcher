# PROGRAM:
S = input()

# Count the number of occurrences of '0' and '1' in S
count_0 = S.count('0')
count_1 = S.count('1')

# Find the minimum possible unbalancedness
min_unbalancedness = abs(count_0 - count_1)

# Print the minimum possible unbalancedness
print(min_unbalancedness)