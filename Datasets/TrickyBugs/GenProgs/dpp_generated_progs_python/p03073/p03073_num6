'''
def min_tiles_repaint(S):
    count = 0 # initialize count
    N = len(S)
    for i in range(1, N):
        # check if two adjacent tiles have the same color
        if S[i] == S[i-1]:
            count += 1 # increment count
    return count

S = input()
print(min_tiles_repaint(S))
'''