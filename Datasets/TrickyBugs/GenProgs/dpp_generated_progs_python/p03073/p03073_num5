# Plan:
1. Initialize a variable `repaint_count` to 0
2. Loop through each tile in the given string `S`
3. If the current tile is not the first tile and is of the same color as the previous tile, increment `repaint_count` by 1
4. Print the final `repaint_count`