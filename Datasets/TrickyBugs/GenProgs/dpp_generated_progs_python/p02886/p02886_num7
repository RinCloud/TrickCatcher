# Getting input
N = int(input())
d = list(map(int, input().split()))

# Calculating sum of health points restored
sum_health_points = 0
for i in range(N):
    for j in range(i+1, N):
        sum_health_points += d[i] * d[j]

# Printing the result
print(sum_health_points)