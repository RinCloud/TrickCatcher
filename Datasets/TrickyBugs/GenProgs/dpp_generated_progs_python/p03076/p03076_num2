A, B, C, D, E = map(int, input().split())

# Calculate the maximum time required for all dishes to be delivered
max_time = max(A, B, C, D, E)

# Calculate the total time required for all dishes to be delivered
total_time = (max_time - 1) // 10 * 10 + max_time

print(total_time)