# Taking input of number of integers
N = int(input())

# Taking input of the integers
a = list(map(int, input().split()))

# Sorting in descending order
a.sort(reverse=True)

# Finding the maximum value of f(m) using the formula
max_value = sum(a) - N

# Printing the maximum value of f
print(max_value)