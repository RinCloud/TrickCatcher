def check_string(S):
    while len(S) > 0:
        if S.endswith("dream") or S.endswith("erase"):
            S = S[:-5]
        elif S.endswith("dreamer") or S.endswith("eraser"):
            S = S[:-7]
        else:
            return "NO"
    return "YES"

S = input()
print(check_string(S))