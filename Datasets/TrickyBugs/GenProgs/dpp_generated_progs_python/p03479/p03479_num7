import sys

def solve(X, Y):
    length = 1
    while X*2 <= Y:
        X *= 2
        length += 1
    return length

if __name__ == "__main__":
    X, Y = map(int, input().split())
    print(solve(X, Y))