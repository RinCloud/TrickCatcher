# Given Input
A = list(map(int, input().split()))

# Sort the tasks in ascending order
A.sort()

# Calculate the minimum cost
min_cost = A[1] - A[0] + A[2] - A[1]

# Print the minimum total cost required to complete all the tasks
print(min_cost)