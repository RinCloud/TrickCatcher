import sys

def min_difference():
    # Read the input
    a, b, c = map(int, sys.stdin.readline().split())

    # Calculate the minimum possible difference
    min_diff = min(a*b*c - a*b, a*b*c - a*c, a*b*c - b*c)

    # Print the result
    print(min_diff)

min_difference()