import math

# Read the input integer
N = int(input())

# Calculate the maximum possible value
max_value = math.floor(N/2) * (math.floor(N/2) + 1)

# If N is odd, add the remaining number
if N % 2 == 1:
    max_value += math.floor(N/2) + 1

# Print the result
print(max_value)