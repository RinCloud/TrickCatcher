import math

def maximum_value(N):
    # calculating the maximum possible value of M_1 + M_2 + ... + M_N
    max_value = math.ceil(N/2)
    
    return max_value

# taking input from the standard input
N = int(input())

# calling the maximum_value() function and printing the result
print(maximum_value(N))