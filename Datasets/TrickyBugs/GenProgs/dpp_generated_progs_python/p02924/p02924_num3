# The maximum value of M1 + M2 + ... + MN can be achieved by choosing a permutation where each M_i is equal to N-1.

# Read the input
N = int(input())

# Calculate the maximum value
max_value = (N-1) * N // 2

# Print the result
print(max_value)