N = int(input())

# Create a list of integers from 1 to N
nums = list(range(1, N+1))

# Sort the list in ascending order
nums.sort()

# Calculate the maximum possible value
max_value = sum([i % p for i, p in enumerate(nums, 1)])

print(max_value)