# Program:

S = input()

count = 0
max_length = 0

for char in S:
    if char in ['A', 'C', 'G', 'T']:
        count += 1
    else:
        count = 0
        
    max_length = max(max_length, count)
    
print(max_length)