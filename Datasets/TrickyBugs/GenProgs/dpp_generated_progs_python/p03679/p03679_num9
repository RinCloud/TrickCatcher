def check_food(X, A, B):
    if B <= A:
        print("delicious")
    elif B - A <= X:
        print("safe")
    else:
        print("dangerous")

X, A, B = map(int, input().split())
check_food(X, A, B)