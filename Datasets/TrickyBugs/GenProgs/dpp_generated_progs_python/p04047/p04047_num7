# getting input N
N = int(input())

# getting input L and converting it into list of integers
L = list(map(int, input().split()))

# sorting the L in ascending order
L.sort()

# summing up the maximum value from each pair of skewers
result = sum(L[::2])

# printing the result
print(result)