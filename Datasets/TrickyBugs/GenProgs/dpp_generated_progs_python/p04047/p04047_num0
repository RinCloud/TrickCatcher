import sys

# Read input
N = int(sys.stdin.readline())
skewers = list(map(int, sys.stdin.readline().split()))

# Sort the skewers in ascending order
skewers.sort()

# Calculate the maximum total number of ingredients
total = sum(skewers[::2])

# Print the result
print(total)