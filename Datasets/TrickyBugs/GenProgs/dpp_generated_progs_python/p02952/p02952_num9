import math

def count_odd_digits(N):
    count = 0
    for i in range(1, N + 1):
        if math.floor(math.log10(i)) % 2 == 0:
            count += 1
    return count

N = int(input())
print(count_odd_digits(N))