# Program to determine whether all characters in a string are different

def check_all_characters_different(S):
    # Create a set to store all unique characters in the string
    unique_chars = set(S)
    
    # Check if the length of the set is equal to the length of the string
    if len(unique_chars) == len(S):
        return "yes"
    else:
        return "no"

# Read the input
S = input()

# Check if all characters in the string are different
result = check_all_characters_different(S)

# Print the result
print(result)