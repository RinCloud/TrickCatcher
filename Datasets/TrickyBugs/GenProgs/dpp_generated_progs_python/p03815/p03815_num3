x = int(input())

# Each rotation gives 7 points, so we can calculate the minimum number of rotations required
# by dividing x by 7 and rounding up to the nearest integer.

result = (x+6) // 7
print(result)