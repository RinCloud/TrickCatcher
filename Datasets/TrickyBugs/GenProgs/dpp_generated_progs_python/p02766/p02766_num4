# Given the number N and base K
N, K = map(int, input().split())

# Initialize the count to 0
count = 0

# Iterate until N is greater than 0
while N > 0:
    # Increment the count
    count += 1
    # Divide N by K
    N //= K

# Print the count
print(count)