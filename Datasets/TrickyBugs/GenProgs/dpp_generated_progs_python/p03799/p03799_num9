import sys

def max_Scc_groups(N, M):
    min_pieces = min(N, M // 2)
    return min_pieces

if __name__ == "__main__":
    input_list = sys.stdin.readline().split()
    N = int(input_list[0])
    M = int(input_list[1])

    result = max_Scc_groups(N, M)
    print(result)