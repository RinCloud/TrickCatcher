# Program

Input = input().split()
N, X = int(Input[0]), int(Input[1])
cities = list(map(int, input().split()))

max_D = 0
for city in cities:
    distance = abs(city - X)
    max_D = max(max_D, distance)

print(max_D)