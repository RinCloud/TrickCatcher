def visit_all_cities():
    N, X = map(int, input().split())
    cities = list(map(int, input().split()))
    
    max_distance = 0
    for city in cities:
        distance = abs(city - X)
        max_distance = max(max_distance, distance)
    
    print(max_distance)

visit_all_cities()