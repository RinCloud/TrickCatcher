def max_kagami_mochi():
    N = int(input())
    diameters = []
    for _ in range(N):
        diameters.append(int(input()))
    
    unique_diameters = set(diameters)
    max_layers = len(unique_diameters)
    print(max_layers)

max_kagami_mochi()