def count_minimum_characters(N, A):
    min_chars = min(A)
    return min_chars


# read the input N
N = int(input())

# read the lengths of the strings A
A = list(map(int, input().split()))

# call the function and print the output
output = count_minimum_characters(N, A)
print(output)