# Given a positive integer N and a string S of length N, we need to determine whether the string is a concatenation of two copies of some string.

# Read the input values
N = int(input())
S = input()

# Check if the string is a concatenation of two copies of some string
if S[:N//2] == S[N//2:]:
    print("Yes")
else:
    print("No")