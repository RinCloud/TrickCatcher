import sys

def can_pay_exact_amount(N, A):
    if N % 500 <= A:
        print("Yes")
    else:
        print("No")

if __name__ == "__main__":
    N = int(sys.stdin.readline().rstrip())
    A = int(sys.stdin.readline().rstrip())
    
    can_pay_exact_amount(N, A)