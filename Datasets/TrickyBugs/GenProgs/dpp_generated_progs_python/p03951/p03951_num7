# Getting input from the user
N = int(input())
s = input().strip()
t = input().strip()

# Finding the length of the shortest string that satisfies the conditions
shortest_length = len(s) + len(t) - N

# Outputting the result
print(shortest_length)