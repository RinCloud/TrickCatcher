# Reading input from Standard Input
N = int(input()) # Reading N
s = input() # Reading s
t = input() # Reading t

# Finding the length of the shortest string
shortest_length = 2 * N - len(set(s) & set(t))

# Printing the length of the shortest string
print(shortest_length)